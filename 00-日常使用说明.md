# 日常使用说明

> 实际使用这套系统的完整指南 — 新手到熟练的全流程

> 首次使用不必通读全文。要接入新项目，先看 [README.md](./README.md) 的“3 分钟快速入口”，再回到这里查具体操作协议。

**版本**: v1.3（工作流对齐版）
**适用**: Codex CLI v0.115.0 附近（2026-03）
**对标**: Claude Code guides v3.14《00-日常使用说明》

---

## 目录

1. [第一次：新项目初始化](#1-第一次新项目初始化)
2. [日常开发流程](#2-日常开发流程)
3. [项目路线图（ROADMAP）](#3-项目路线图roadmap)
4. [Git 操作说明](#4-git-操作说明)
5. [上下文管理：何时用 $handoff $catchup /clear](#5-上下文管理)
6. [新开会话 vs 继续任务](#6-新开会话-vs-继续任务)
7. [与 Claude Code 的操作差异](#7-与-claude-code-的操作差异)
8. [所有命令速查](#8-所有命令速查)
9. [常见场景处理](#9-常见场景处理)

---

## 1. 第一次：新项目初始化

只需要做一次，之后就不用管了。

```bash
# 1. 进入项目根目录，启动 Codex
codex

# 2. 推荐直接使用初始化 prompt
"请读取 /Users/stephen/Downloads/00_project/guides-codex/prompt-新项目初始化.md，
并按里面的 Phase 帮我完成初始化。
我的项目技术栈是：[前端 React + 后端 FastAPI + PostgreSQL，pnpm 管理]"
```

Codex 会自动完成：
- 运行 `/init` 生成 `AGENTS.md` 草稿
- 创建 `.codex/config.toml`（审批策略 + 沙盒 + MCP 配置）
- 创建 `.codex/rules/default.rules`（ExecPolicy 命令策略）
- 创建 `.agents/skills/` 目录（audit、deep-audit、catchup、handoff、spec、task、done、docs、release、diagnose）
- 按需创建 `.codex/agents/` 下的 reviewer / docs-researcher 预设
- 按需创建子目录 `AGENTS.md`
- 按需补 `docs/roadmap/`、`docs/specs/`、`docs/architecture/`、`docs/development/`

你只需要：
- 检查 `AGENTS.md` 内容是否准确，控制在 8 KiB 以内
- 标记项目为信任（首次加载项目级 config 需要）：

```toml
# ~/.codex/config.toml
[projects."/path/to/your/project"]
trust_level = "trusted"
```

### 初始化接入验收标准

第一次接入完成后，不要只看“文件都生成了”，而要确认下面 6 件事：

1. `AGENTS.md` 里的命令和项目真实命令一致，不是模板占位
2. `.codex/rules/default.rules` 已区分 `allow` / `prompt` / `forbidden`
3. 至少一个真实项目命令已经跑通过
4. `docs/roadmap/README.md` 已存在，后续有地方落长期进度
5. 至少 `catchup` / `handoff` / `spec` / `task` / `done` / `docs` / `release` / `diagnose` 这些高频入口已经有最小可用契约
6. 你自己能说清楚：下次进入这个项目时，应该怎么开始工作

如果这 6 条里有 2 条以上做不到，就说明初始化还没真正完成，只是“生成了文件”。

---

## 2. 日常开发流程

### 正常的一天

```
打开终端
   ↓
codex                           ← 启动 Codex
 或 codex resume --last         ← 或恢复上次会话
   ↓
AGENTS.md 自动加载              ← 项目规范、完成标准都在里面
   ↓
直接说需求，开始开发             ← "帮我实现用户登录功能"
   ↓
Codex 自动：
  - 先探索相关代码（不直接写）
  - 提出方案，等你确认
  - 实现代码（TDD：先写测试 → 实现 → 验证）
  - 验证：跑测试 + 边界检查 + lint（AGENTS.md 完成标准驱动）
   ↓
/review                         ← 代码审查
   ↓
$done / $docs / $handoff        ← 同步状态、刷新文档或交接
   ↓
git commit                      ← ExecPolicy prompt 确认后提交
   ↓
继续下一个功能，或关闭
```

**你需要手动做的事**：
- `/review`：每个功能完成后手动触发代码审查
- `$release`：阶段或版本整理时手动触发
- `git push` 确认：ExecPolicy `prompt` 规则会弹出确认
- 纠正后更新 AGENTS.md：Codex 没有 Auto Memory，纠正不会自动记住

### 日常命令分工

- `$task`：小任务直接闭环
- `$spec`：跨会话、多决策点改动的实施入口
- `$done`：功能或切片完成后的状态收尾
- `$docs`：文档刷新、starter 回写、入口一致性修正
- `$release`：阶段或版本整理
- `$diagnose`：流程摩擦、结构问题、长期维护风险诊断

> **与 Claude Code 的关键差异**：Codex 不是“没有自动化”，而是自动化更分散。你通常要组合 `AGENTS.md`、Hooks、Rules / ExecPolicy、Skills 和 CI/CD，而不是依赖单一机制。

---

### 开发一个功能的标准节奏

```
你：帮我实现 [功能描述]

Codex：（自动进入探索）
  读取相关文件...
  当前代码结构是这样的...
  我有 2 个实现方案：方案 A... 方案 B...
  你更倾向于哪个？

你：用方案 A

Codex：好，按方案 A 实施，先写测试...
  [TDD：写测试 → 实现代码 → 测试通过]
  [Verify：边界条件检查 → lint/类型检查 → 全量测试]
  验证全部通过，功能已实现。

你：/review

Codex：[启动审查代理 → 分析变更]
  审查完成，发现 2 个问题并已修复。

你：$done

Codex：已同步 roadmap/spec 状态，当前切片可提交。

  git commit -m "feat(auth): 实现用户登录功能"
  （ExecPolicy prompt → 等待你确认）

你：确认

Codex：commit 已创建。

你：push 一下

Codex：执行 git push... （ExecPolicy prompt → 确认）

你：确认

Codex：推送完成。
```

### 第一次正式做功能：从讨论到实施

配置完成后，第一次做功能时，建议严格按这个顺序走：

1. 先说目标，不让 Codex 立刻写代码
2. 让 Codex Explore 相关代码和现状
3. 让 Codex 给出方案或风险点
4. 判断这是“小需求 / 中需求 / 大需求”
5. 只有在分流判断完成后，才进入 Code 或 `$spec`

推荐起手提示：

```text
我们要做 [功能名]。
先不要写代码，先探索相关实现、现有约束和风险，再告诉我应该怎么做。
```

#### 分流规则

| 类型 | 特征 | 下一步 |
|------|------|--------|
| 小需求 | 单文件或少量文件、方向明确、无需长期记录 | 直接进入 Code |
| 中需求 | 涉及多个文件、需要一点方案比较、但当天能闭环 | Explore → Plan → Code |
| 大需求 | 讨论超过 2 轮、涉及架构/权限/数据模型、会跨上下文或跨提交 | 先 `$spec`，再实施 |

#### 什么时候不要急着写代码

下面任意一条成立，都应该先继续讨论或先写 spec：

- 你还说不清功能边界
- Codex 给出了 2 个以上明显不同的方案
- 这个功能会改数据库、认证、权限、接口契约
- 这个功能不是一个提交能完成的
- 你已经意识到中途大概率要 `/clear` 或隔天继续

#### 什么时候可以直接开始实现

下面大多成立时，可以跳过 spec，直接进实施：

- 改动目标单一
- 影响范围很清楚
- 验证方式很清楚
- 今天大概率能做完并审查提交

一个稳妥的实施前确认提示是：

```text
按你刚才的理解，先列出会改哪些文件、先后顺序和验证方式。
确认后再开始写代码。
```

---

## 3. 项目路线图（ROADMAP）

### 是什么

`docs/roadmap/` 目录是项目的持久化进度跟踪系统，解决"Codex 跨会话不知道项目做到哪了"的问题。

```
docs/roadmap/
├── README.md              ← 总览：Phase 列表 + 当前聚焦的 Phase
├── phase-1-基础功能.md     ← 已完成的 Phase
├── phase-2-核心业务.md     ← 当前进行中
└── phase-3-高级功能.md     ← 未开始
```

### 你需要做什么

| 场景 | 谁来做 |
|------|--------|
| 初始化路线图 | Codex 探索项目后自动生成，你确认内容 |
| 更新进度（checkbox） | `$handoff` 时 Codex 自动更新 |
| 添加新功能 | 你在对话中说"加到路线图里"，Codex 更新 |
| 切换 Phase | 手动更新 AGENTS.md 引用 |

### AGENTS.md 中的引用

> **注意**：Codex 没有 Claude Code 的 `@path` 引用语法。替代方案是在 AGENTS.md 中明确写出路线图信息，或在完成标准中指示 Codex 主动读取：

```markdown
## 路线图

当前 Phase：Phase 2 核心业务（docs/roadmap/phase-2-核心业务.md）
完成功能后，读取对应 Phase 文件并更新 checkbox 状态。
```

---

## 4. Git 操作说明

### Codex 的 Git 操作与 ExecPolicy 控制

| 操作 | ExecPolicy 建议 | 是否需要你确认 |
|------|-----------------|--------------|
| `git status/log/diff` | `allow` | 否 |
| `git add <文件>` | `allow` | 否 |
| `git commit` | `prompt` | **是** |
| `git push` | `prompt` | **是** |
| `git push --force` | `forbidden` | 直接禁止 |
| `git reset --hard` | `forbidden` | 直接禁止 |

### 关于 git add

在 AGENTS.md 中约束 Codex 使用具体文件路径：

```markdown
- Always use specific file paths with `git add`; never use `git add .`
```

### 完整的 ExecPolicy 配置

参见 `02-自动化与命令策略.md` 中的完整 ExecPolicy 示例。

---

## 5. 上下文管理

### 什么是上下文满了

Codex 每个会话有固定大小的上下文窗口。随着对话越来越长，窗口会被填满。满了之后 Codex 的表现会变差。

### 如何查看上下文用量

```bash
/status              # Token 使用统计
/statusline          # 自定义状态栏（可配置 context-remaining）
```

### 三个阶段的处理方式

```
0% - 70%   正常工作，不用管
            ↓
70% - 85%  开始规划会话边界
           "我们当前做到哪了？还有什么没完成？"
            ↓
           $handoff          ← 让 Codex 把进度写入文件
           /clear            ← 清空上下文
           $catchup          ← 读取进度文件，恢复状态
            ↓
85%+       质量会明显下降，此时才处理已经晚了
           （所以要在 70% 时主动处理）
```

### $handoff → /clear → $catchup 的具体操作

```bash
# Step 1：保存进度
$handoff
# Codex 会把"做到哪里、决策了什么、还剩什么"写到 .agents/session-notes.md
# 同时自动提交当前变更、更新路线图

# Step 2：清空上下文
/clear

# Step 3：恢复（三种方式，按推荐排序）
$catchup                    # 读取 session-notes.md + 项目状态
codex resume --last         # 或恢复完整会话历史
"读取 .agents/session-notes.md，继续之前的工作"   # 最简单的手动恢复
```

整个过程大约 1-2 分钟，之后可以继续工作。

### 项目配置升级与迁移

当 guides 本身更新，或者你要把旧项目接到这套体系时，优先直接使用现成 prompt：

```text
升级已有 Codex 项目：
读取 /Users/stephen/Downloads/00_project/guides-codex/prompt-guide版本升级.md

迁移旧 AI 协作体系：
读取 /Users/stephen/Downloads/00_project/guides-codex/prompt-旧项目迁移.md

更新 guides-codex 本身：
读取 /Users/stephen/Downloads/00_project/guides-codex/prompt-更新指南规范.md
```

---

## 6. 新开会话 vs 继续任务

### 场景一：新开会话，开始新功能

```bash
# 昨天的功能已经 commit 了
# 今天开始新的功能

codex                # 启动
# AGENTS.md 自动加载，Codex 知道项目规范

"今天我们来做用户权限管理功能"    # 直接开始
```

**不需要 `$catchup`** — AGENTS.md 已经提供了项目规范，新功能不需要恢复进度。

> **与 Claude Code 的差异**：Claude Code 有 Auto Memory 自动记住你的偏好和纠正。Codex 没有 — 如果之前纠正了 Codex 的某些行为（如"不要用 Redux"），**必须**已经写入 AGENTS.md，否则会重复犯错。

---

### 场景二：新开会话，继续昨天未完成的任务

```bash
# 昨天任务做到一半，执行了 $handoff 后关闭

# 方式 A（推荐）：恢复完整对话历史
codex resume --last

# 方式 B：重新开始，用 catchup 恢复进度
codex
$catchup              # 读取昨天的 session-notes.md
```

---

### 场景三：会话中上下文接近满了

```bash
# /status 显示接近 70%

$handoff              # 保存进度
/clear                # 清空
$catchup              # 恢复，继续工作
```

---

### 判断要不要恢复的规则

```
上次关闭前有没有运行 $handoff？
  ├── 没有 → 不需要恢复，直接开始
  └── 有   → 要不要继续那个任务？
              ├── 不继续 → 不需要恢复
              └── 继续   → codex resume --last 或 $catchup
```

---

## 7. 与 Claude Code 的操作差异

### 日常操作对照

| 操作 | Claude Code | Codex CLI |
|------|------------|-----------|
| 启动会话 | `claude`（Hook 自动运行） | `codex` 或 `codex resume --last` |
| 上下文恢复 | Auto Memory 自动 | **手动** `resume` 或 `$catchup` |
| 开发验证 | Hook 自动触发 | AGENTS.md 完成标准驱动（软约束） |
| 代码审查 | `/simplify`（三维并行） | `/review`（审查预设） |
| 自动格式化 | PostToolUse Hook | AGENTS.md 指令（软约束） |
| 测试门禁 | PreToolUse Hook | ExecPolicy `prompt` + AGENTS.md |
| 完成通知 | Stop Hook | `notify` 配置 |
| 纠正记忆 | Auto Memory 自动记住 | **手动写入 AGENTS.md** |
| 批量变更 | `/batch` | Subagents / 多线程拆分 / Git Worktree |
| 定时重复 | `/loop` | 无等价 |
| 配置切换 | 无 | `--profile` |
| 远程任务 | 无 | 远程环境 / Web 长任务 |

### 需要养成的新习惯

1. **纠正后更新 AGENTS.md** — 每次纠正 Codex 的行为后，立即在 AGENTS.md 中添加 Always/Never 约束
2. **手动触发审查** — `/review` 需要手动调用，不像 Claude Code 的 `/simplify` 那样可以集成到自动流程
3. **手动恢复会话** — `codex resume --last` 而非依赖 Auto Memory
4. **利用 Profiles** — 针对不同场景切换配置，这是 Codex 的独有优势

---

## 8. 所有命令速查

### 高频命令

| 命令 | 何时用 |
|------|--------|
| `/review` | **每次功能完成后**（代码审查） |
| `$done` | 功能完成后收尾检查（验证 Roadmap/Spec 同步） |
| `$handoff` | 任务做一半要中断；或上下文接近 70% |
| `$catchup` | 继续未完成的任务；或 /clear 后恢复 |
| `$spec` | 需求讨论到一定程度后，整理为设计文档 |
| `$task` | 方向明确、当前会话能闭环的小任务 |
| `$docs` | 文档刷新、starter 回写、入口修正 |
| `$release` | 阶段或版本整理 |
| `$diagnose` | 结构、流程和长期维护风险诊断 |
| `/clear` | 配合 $handoff 使用，重置上下文 |
| `$audit` | 每周健康检查 |
| `$audit --quick` | 快速检查 |
| `$audit --security` | 上线前安全扫描 |
| `$deep-audit` | Phase 完成后全面审计 |
| `/plan` | 复杂任务前先规划 |
| `/status` | 查看 Token 使用量 |

### 系统命令

| 命令 | 用途 |
|------|------|
| `/plan` | 进入 Plan Mode |
| `/review` | 代码审查 |
| `/model` | 切换模型 |
| `/permissions` | 运行时切换审批模式 |
| `/personality` | 切换沟通风格（friendly/pragmatic/none） |
| `/skills` | 浏览可用 Skills |
| `/mcp` | 管理 MCP 连接 |
| `/diff` | 查看代码变更 |
| `/fork` | 分支对话，探索不同方案 |
| `/new` | 新对话（不清屏） |
| `/clear` | 清空上下文 |
| `/compact` | 有损压缩（优先用 Document-and-Clear） |
| `/agent` | 切换代理线程 |
| `/mention` | 附加文件到对话 |
| `/resume` | 恢复保存的对话 |
| `/statusline` | 自定义状态栏 |
| `/debug-config` | 配置诊断 |
| `/init` | 生成 AGENTS.md |

### 自定义 Skills（$前缀）

| 命令 | 用途 |
|------|------|
| `$catchup` | 清空上下文后恢复状态 |
| `$handoff` | 提交变更 + 生成交接文档 |
| `$spec` | 讨论成果整理为设计文档 |
| `$task` | 小任务直接闭环 |
| `$done` | 功能完成收尾检查 |
| `$docs` | 文档刷新和契约回写 |
| `$release` | 阶段或版本整理 |
| `$diagnose` | 工作流和结构性健康诊断 |
| `$audit` | 项目健康检查 |
| `$deep-audit` | 全面深度审计 |
| `$skill-creator` | 创建新 Skill（内置） |
| `$skill-installer` | 安装社区 Skill（内置） |

### 快捷键

| 快捷键 | 用途 |
|--------|------|
| `Ctrl+G` | 在 `$VISUAL`/`$EDITOR` 中编辑长提示 |
| `Ctrl+L` | 清屏 |
| `Ctrl+C` | 退出/中断 |
| `!command` | 执行 shell 命令 |
| `@filename` | 模糊搜索附加文件 |

### CLI 启动参数

| 参数 | 用途 |
|------|------|
| `codex` | 正常启动 |
| `codex resume --last` | 恢复最近会话 |
| `codex --profile deep-review` | 用指定 Profile 启动 |
| `codex --full-auto` | 全自动模式 |
| `codex -i image.png "描述"` | 附带图片 |
| `codex exec "任务"` | 非交互式执行 |
| `codex exec --full-auto --json "任务"` | CI/CD 自动化 |

---

## 9. 常见场景处理

### 场景：任务做到一半，临时有事要离开

```bash
$handoff              # 保存进度 + 提交变更
# 直接关闭终端

# 回来后
codex resume --last   # 恢复完整对话
# 或
codex
$catchup              # 接着做
```

---

### 场景：上下文满了，任务还没做完

```bash
$handoff              # 保存进度到 .agents/session-notes.md
/clear                # 清空上下文
$catchup              # 恢复，继续做
```

---

### 场景：今天的功能全部做完了，要收工

```bash
# 如果想确认没有遗漏：
$done                   # 可选：收尾检查

"push 一下"            # Codex 执行 git push（ExecPolicy 弹出确认）
# 确认后，直接关闭
# 不需要 $handoff（功能已完整 commit，明天从新功能开始）
```

---

### 场景：今天做到一半，要收工，明天继续

```bash
"先把当前改动 commit 一下，用 WIP 前缀"
# Codex commit：wip: 登录功能开发中

$handoff              # 记录"做到哪一步，明天继续什么"
# 关闭

# 明天
codex resume --last   # 恢复昨天对话
# 或
codex
$catchup              # 读取交接文档，接着做
```

---

### 场景：修复一个 Bug

```bash
# Bug 修复复用同一套开发循环

你："登录页面在手机端点击提交按钮没有反应"

# Codex 自动进入 Explore（复现+定位）：
#   读取登录组件、事件处理逻辑...
#   定位根因：onClick 使用了 onMouseDown，移动端不触发

你：确认，修一下

# Codex 进入 Code（回归测试+修复）：
#   1. 写复现测试
#   2. 修复：onMouseDown → onPointerDown
#   3. 复现测试变绿
#   4. Verify 执行（完成标准驱动）

# git commit -m "fix(auth): 修复移动端登录按钮无响应"
```

---

### 场景：发现 Codex 在重复犯同一个错误

```bash
# 例如：Codex 总是用 Redux，但你的项目用 Zustand

# 直接更新 AGENTS.md（这是唯一可靠的方式）：
# 在 AGENTS.md 的约束里加一条：
# "Never use Redux; always use Zustand for state management"

# 注意：与 Claude Code 不同，Codex 没有 Auto Memory
# 口头纠正不会跨会话记住，必须写入 AGENTS.md
```

---

### 场景：需求讨论了很多轮，上下文快满了

```bash
# 讨论了好几轮需求、调研了技术方案...
# 上下文已经用了 50-60%，还没开始写代码

$spec user-auth       # 将讨论成果整理为 docs/specs/user-auth.md
/clear                # 清空上下文

# 如果还要继续讨论
"读取 docs/specs/user-auth.md，继续讨论权限模型部分"
# 讨论后再次 $spec 更新

# 如果讨论完了，开始实施
"读取 docs/specs/user-auth.md，开始实施"
```

> `$spec` 支持增量更新——讨论可以跨多个上下文窗口。

---

### 场景：复杂功能，不知道从哪下手

```bash
/plan                 # 进入 Plan Mode

"我需要实现一个完整的权限管理系统，
 包括角色、权限、用户绑定，支持动态菜单。
 请先探索现有代码，然后给我一个实现方案，不要写代码。"

# Codex 探索代码，给出方案
# 你确认方案后

# 此时上下文可能已经 50-70%
# 推荐：主动清理上下文，带 plan 开新会话
"将实施方案写入 .agents/plan.md"
/clear
"读取 .agents/plan.md，按方案实施"

# 如果上下文还充裕（< 40%），可以直接继续
```

---

### 场景：不同任务需要不同配置

```bash
# 复杂架构设计用高配
codex --profile deep-review

# 简单格式修改省 Token
codex --profile lightweight

# CI/CD 自动化
codex exec --full-auto "修复测试"
```

---

### 场景：每周健康检查

```bash
$audit                # 检查代码质量、依赖、文档同步
# 查看报告，按优先级处理问题
```

---

### 场景：某个 Phase 功能全部完成

```bash
$deep-audit           # 全面审计：代码、文档、依赖全部对一遍
# Codex 自动修复问题并提交报告
```

---

### 场景：想把长任务放到远程环境

```text
推荐思路：
1. 本地先 Explore / Plan，明确边界和约束
2. 将清晰、可独立推进的任务交给远程环境或 Web
3. 本地继续做 review、集成、验证和最终提交
```

> 这部分的具体入口以 Codex Web、Environments、Internet Access 和最新 changelog 为准，不再以旧版 `codex cloud` 命令草稿为主。

---

## 10. 完整流程演练

下面用一个真实感较强的例子，把这套系统从接入到收尾完整走一遍。

### 场景

你要在一个已有项目中实现“用户登录 + 权限控制”。

项目现状：
- 已有基础前后端代码
- 还没有 Codex 协作配置
- 这个功能不是一小时能做完的小改动

### Step 1：接入项目

进入项目目录后启动 Codex：

```text
读取 /Users/stephen/Downloads/00_project/guides-codex/prompt-新项目初始化.md，
按里面的 Phase 帮我配置当前项目。
技术栈是：React + FastAPI + PostgreSQL + pnpm。
```

Codex 会先探索项目，再输出项目画像和设计方案。你确认后，它会创建：
- `AGENTS.md`
- `.codex/config.toml`
- `.codex/rules/default.rules`
- `.agents/skills/`
- `docs/roadmap/`

这一步的产出，不是“让 AI 更聪明”，而是把以后每次会话都会用到的工程规则固定下来。

### Step 2：把需求讨论落盘

配置完成后，不要立刻让 Codex直接写代码。先讨论需求和边界：

```text
我们要做用户登录和权限控制。
先不要写代码，先探索现有认证逻辑、数据库模型和前端路由守卫。
```

Codex 会先读代码，给出当前结构和可选方案。

如果讨论超过几轮，或者你已经有明确方案，就执行：

```text
$spec auth-and-rbac
```

让 Codex 把讨论结果整理到 `docs/specs/auth-and-rbac.md`。

这样做的目的，是把“需求讨论阶段”和“实施阶段”切开，避免全部挤在一个上下文窗口里。

什么时候应该从讨论切到 `$spec`：
- 同一个需求已经讨论了 2 轮以上
- 已经出现多个可选方案，需要沉淀决策
- 你准备 `/clear`，但又不想丢掉讨论成果
- 这个功能会跨多个提交或多个上下文窗口
- 这个功能需要 roadmap、状态同步或后续交接

如果以上都不满足，通常还可以继续直接讨论，不必强行生成 spec。

更准确地说，`$spec` 的目标不是记录“我们聊过什么”，而是产出一个“足够开始实施”的入口文件。一个合格的 spec，至少要回答这 4 个问题：

1. 这次要解决什么问题
2. 这次不解决什么
3. 预计会改哪些模块或文件
4. 实施完成后，如何判断它算完成

如果这 4 个问题还答不出来，继续讨论比急着生成 spec 更合理。

反过来，如果这 4 个问题已经答出来了，却还在继续空转讨论，通常就应该立刻 `$spec`，然后进入实施。

一个简单的判定法：

```text
这个功能能不能开始写了？
  ├── 不能，还缺核心决策 → 继续讨论
  ├── 能，但容易忘掉决策/要跨上下文 → 先 $spec，再写
  └── 能，而且很小、很直接 → 可以不写 spec，直接实施
```

这一段的关键不是“凡复杂功能都必须写 spec”，而是“凡需要跨上下文保持决策的功能，都应该有 spec”。

### Step 3：进入实施

接着让 Codex按 spec 开始：

```text
读取 docs/specs/auth-and-rbac.md，开始实施。
先做登录链路，不要一次改完全部权限系统。
```

这时推荐按功能切片，而不是一次性铺开。一个比较稳的节奏是：
- 先补测试
- 再改后端认证
- 再改前端登录流程
- 最后补权限守卫

这里最重要的不是“开始写代码”这四个字，而是**从 spec 切到第一个可执行切片**。也就是说，spec 有了之后，不要再继续泛泛地聊，而要马上把问题收缩成：

1. 第一批只改什么
2. 第一批明确不改什么
3. 第一批的完成标志是什么

一个好的实施起手提示，应该像这样：

```text
读取 docs/specs/auth-and-rbac.md，开始实施。
第一批只做登录链路：
- 后端登录接口
- 前端登录表单提交
- 基础登录态保存
这一次先不要做 RBAC、动态菜单和后台权限配置。
完成标志：用户可以成功登录，相关测试通过。
```

这样做的好处是：
- spec 负责保存整体方向
- 当前对话只负责推进一个切片
- 即使中途 `/clear` 或换会话，也不会重新散掉

从 spec 进入实施时，推荐固定问 Codex 这 3 个问题：

1. 第一批你准备改哪些文件
2. 第一批你不改哪些部分
3. 你准备先写哪些测试或验证

只要这 3 个问题答不清楚，说明还没有真正进入实施，而是还停留在讨论阶段。

如果任务复杂，也可以先：

```text
/plan
```

确认方案后让 Codex把实施计划写到 `.agents/plan.md`，然后 `/clear` 再带 plan 开新一轮实施。

什么时候 spec 后还需要再 `/plan`：
- spec 定了目标，但实施路径仍不清楚
- 预计会改很多文件，容易失控
- 有多个实现分支，需要先收敛
- 你已经感觉“可以开始做了，但不知道第一刀落哪里”

什么时候 spec 后可以直接实施：
- 第一批改动范围很清楚
- 影响文件不多
- 测试与验证路径明确
- 你能用一两句话说明这一批的完成标志

进入实施后，下一件最重要的事不是“尽量多做一点”，而是**控制单次实施单元的大小**。

推荐的切片原则不是按“模块名”切，也不是按“文件数”切，而是按“这一轮能不能完整验证并收尾”切。一个好的单次实施单元，应该满足：

1. 只服务一个明确结果
2. 改完后可以立即验证
3. 验证通过后可以进入 review
4. 即使中断，也容易 handoff

例如这 3 种切片方式里，通常只有第一种是好的：

```text
好切片：
实现登录接口并让登录测试通过

偏大切片：
把认证系统全部做完

偏乱切片：
顺手把用户中心、权限菜单、登录页 UI 一起改了
```

一个简单判断法：

```text
这一轮做完后，我能不能明确说“它已经可验证”？
  ├── 不能 → 切片还太大
  └── 能   → 可以开始做
```

对 Codex 来说，最稳的实施单位通常是：
- 一个接口
- 一个页面提交链路
- 一个状态流转
- 一个 bug 根因修复
- 一个明确的权限判断点

而不是：
- 一个完整子系统
- 一整个 Phase
- 多个前后端主题一起推进

如果 Codex开始把切片做大了，最直接的拉回方式就是补一句：

```text
这一轮只做一个可验证切片，做完就停下来汇报，不要顺手扩展。
```

### Step 4：验证

代码写完后，不要直接 commit。应让 Codex按 `AGENTS.md` 的完成标准去做：
- 相关测试
- lint / typecheck
- 边界情况检查
- 回归检查

如果项目配置了 Hooks、Rules 和 CI，这一步会更稳；但即便没有全自动，也应该把这一步当成硬习惯。

这里最重要的优化是：**Verify 必须产出结果，而不是一句“已验证”。**

一个合格的 Verify 汇报，至少应该包含：

1. 实际运行了哪些命令
2. 哪些命令没运行，以及原因
3. 检查了哪些边界条件
4. 当前还剩什么风险

推荐让 Codex 用这种格式汇报：

```text
Verify 结果：
- 已运行：pnpm test --filter auth
- 已运行：pnpm lint
- 未运行：全量 e2e（本地环境未启动）
- 已检查边界：空密码、错误密码、未激活用户
- 当前剩余风险：未覆盖真实短信登录链路
```

这样做的好处是：
- 你能马上判断验证是否充分
- `/review` 有清晰上下文
- handoff 时不会丢掉“哪些已经验过”

什么时候 Verify 不算完成：
- 只说“测试通过了”，但没说跑了什么
- 只跑了单测，完全没看边界
- 明明没法运行某项验证，却没有显式说明
- 有明显剩余风险，但没有被写出来

一个最简单的要求是：

```text
不要只说“已验证”。
请列出：
1. 跑了什么
2. 没跑什么
3. 边界检查了什么
4. 剩余风险是什么
```

如果这 4 条说不清，通常就还不应该进入 `/review`，更不应该 commit。

### Step 5：代码审查

验证通过后，执行：

```text
/review
```

如果你担心安全问题，可以补充审查目标：

```text
/review security
```

这一步相当于 Claude 体系里的 `/simplify` 位置，但 Codex 的做法更接近“审查预设 + 子代理协作”。

这里也不要把 `/review` 当成一个“形式上的最后步骤”。一个有价值的 review，至少要回答：

1. 这次改动最可能出错的地方在哪
2. 有没有行为回归风险
3. 有没有漏掉的测试或边界
4. 有没有不该顺手带进来的额外改动

推荐让 Codex 用这种格式汇报 review 结果：

```text
Review 结果：
- 发现问题：2 个
- 已直接修复：1 个
- 仍建议人工确认：1 个
- 主要风险：权限缓存可能导致旧角色短暂生效
- 建议补充：增加角色切换后的回归测试
```

什么时候 review 不算完成：
- 只说“看起来没问题”
- 没指出任何风险点
- 没区分“已修复”和“仍待确认”
- 明明是高风险改动，却没有建议额外验证

一句话要求就是：

```text
不要只给结论，要列出发现、修复、剩余风险和建议补测项。
```

### Step 6：同步项目状态

功能做完后，不只是 commit 代码，还要同步项目文档状态。

这里推荐：

```text
$done
```

或者在长任务中断时：

```text
$handoff
```

这些动作的目标是同步：
- `docs/roadmap/` 当前 Phase 的 checkbox
- `docs/specs/` 中当前 spec 的状态
- `.agents/session-notes.md` 中的交接信息

这一层是 Codex 替代 Claude Auto Memory 的关键做法。不是靠“它自己记住”，而是靠项目文件把状态显式持久化。

这里的关键优化是：**状态同步应该发生在 commit 前，而不是事后想起来再补。**

推荐的顺序是：

```text
Verify
→ /review
→ $done 或 $handoff
→ git commit
```

这样做的原因很简单：
- commit 前，Codex 还保留着完整上下文
- roadmap / spec 更容易和实际改动保持一致
- handoff 文件更不容易漏掉关键决策

`$done` 更适合：
- 一个功能切片已经闭环
- 需要同步 roadmap / spec 状态
- 准备进入 commit

`$handoff` 更适合：
- 任务还没闭环
- 即将 `/clear` 或离开
- 需要把“做到哪、下一步做什么”写下来

如果 Codex做完代码后直接想 commit，可以用一句话把它拉回：

```text
先不要 commit，先同步 roadmap/spec/session-notes，再汇报。
```

### Step 7：提交与中断恢复

如果功能已经完整闭环：
- `/review`
- `$done`
- `git commit`
- 需要时 `git push`

如果功能做到一半必须中断：

```text
$handoff
/clear
```

回来时：

```text
codex resume --last
```

或者：

```text
$catchup
```

这里再补一个 commit 前的最小门槛。只有同时满足下面 4 条，才应该进入 commit：

1. 当前切片已经可验证
2. Verify 已明确汇报结果
3. Review 已明确汇报风险
4. roadmap / spec / session-notes 已同步到位

如果这 4 条里缺 2 条以上，通常不应该 commit，而应该继续补验证或补状态同步。

push 也不应该和 commit 混成一步。更稳的顺序是：

```text
先 commit
再确认本地状态
最后再 push
```

### Step 8：阶段收尾

当一个 roadmap phase 做完，不要只看代码是否 merged。还要做阶段性收尾：

```text
$deep-audit
```

必要时再刷新架构、spec、roadmap 和开发文档。

这里的关键不是“再做一次大检查”，而是确认这个 Phase 是否真的可以退出。一个合格的 Phase 收尾，至少要确认：

1. 当前 Phase 的 roadmap 已更新
2. 相关 spec 状态已经收束
3. 没有明显遗留的高优先级风险
4. 下一个 Phase 的入口已经清楚

如果 Phase 收尾做完后，团队下次再进项目仍然说不清“现在做到哪了、接下来做什么”，说明这次收尾其实没有完成。

### Step 9：长期维护循环

开发流程不是在 merge 时结束。项目长期运行后，还需要两个慢周期动作：

1. 项目配置升级  
   当 guides 更新后，用 `prompt-guide版本升级.md` 对项目做增量升级。

2. guides 自身维护  
   当 Codex 官方能力变化后，用 `prompt-更新指南规范.md` 更新本仓库本身。

也就是说，这套系统最终形成的是一个闭环：

```text
guides 更新
→ 项目配置升级
→ 日常开发
→ Phase 收尾
→ 再回到 guides 维护
```

### 这一整套流程背后的逻辑

整套体系的真正目标，是把开发拆成 6 层：

1. 规范层：`AGENTS.md`
2. 行为层：Hooks、Rules、ExecPolicy
3. 工作流层：Skills
4. 状态层：roadmap、spec、session-notes
5. 验证层：review、测试、CI/CD
6. 维护层：项目升级与 guides 自身演进

你平时感受到的“开发体验”，其实就是这 6 层一起工作后的结果。

---

## 附：Codex CLI 日常操作总结

### 每天必做

```
启动：codex / codex resume --last
开发：直接说需求
审查：/review（每个功能完成后）
提交：git commit（ExecPolicy 确认）
```

### 按需操作

```
上下文管理：$handoff → /clear → $catchup
纠正行为：更新 AGENTS.md（立即！）
收尾检查：$done
讨论落盘：$spec
```

### 每周

```
健康检查：$audit
检查 AGENTS.md 是否需要更新
```

### Phase 完成后

```
全面审计：$deep-audit
```

---

**版本**: v1.3（工作流对齐版）
**更新日期**: 2026-03-31
