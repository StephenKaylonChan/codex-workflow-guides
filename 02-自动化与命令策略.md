# 自动化与命令策略

> Codex CLI 的自动化能力全景 — Hooks、Rules、通知、AGENTS.md 与 CI/CD 的分层组合

**版本**: v1.3（工作流对齐版）
**适用**: Codex CLI v0.115.0 附近（2026-03）
**对标**: Claude Code guides v3.14《02-Hooks自动化配置》

---

## 目录

1. [与 Claude Code Hooks 的差异总览](#1-与-claude-code-hooks-的差异总览)
2. [自动化能力全景](#2-自动化能力全景)
3. [ExecPolicy 命令策略（硬约束）](#3-execpolicy-命令策略硬约束)
4. [通知系统（硬约束）](#4-通知系统硬约束)
5. [AGENTS.md 工作流约束（软约束）](#5-agentsmd-工作流约束软约束)
6. [上下文自动管理](#6-上下文自动管理)
7. [CI/CD 自动化（硬约束）](#7-cicd-自动化硬约束)
8. [MCP 服务器模式（编程控制）](#8-mcp-服务器模式编程控制)
9. [常见场景实现方案](#9-常见场景实现方案)
10. [当前局限与未来展望](#10-当前局限与未来展望)

---

## 1. 与 Claude Code Hooks 的差异总览

> **2026-03-28 复核结论**：Codex 已经有官方 Hooks 文档与配置能力，但它不是 Claude Code Hooks 的一比一替身。

Claude Code 的 Hooks 体系更完整，事件密度更高，也更偏“产品内建自动化”。Codex 则更像把自动化拆成几层原语：Hooks 负责生命周期动作，Rules / ExecPolicy 负责命令级门禁，`notify` 负责提醒，`AGENTS.md` 负责软约束，CI/CD 负责最终硬验证。

### 对照表

| 维度 | Claude Code | Codex CLI（复核后） | 执行保证 |
|------|-------------|------------------------|---------|
| 生命周期 Hooks | 事件更多、更完整 | 官方已提供 Hooks；适合前后置脚本、通知、部分门禁 | 中到高 |
| 命令执行前拦截 | Hook + permissions | **Rules / ExecPolicy** 更强，适合 allow/prompt/forbidden | 高 |
| 写文件后自动动作 | PostToolUse 很强 | 可优先尝试 Hooks；不足部分再用 AGENTS.md + CI | 中 |
| 完成通知 | Hook | `notify` + Hooks | 中到高 |
| 每次 prompt 前注入上下文 | 能力更强 | 当前仍主要依赖 `AGENTS.md`、子目录文档、resume | 低到中 |
| 技能内临时 Hook | 支持 | 当前官方文档未见直接等价 | — |

### 执行保证级别说明

| 级别 | 含义 | 示例 |
|------|------|------|
| **硬约束** | 程序化强制执行，模型无法绕过 | ExecPolicy 的 `forbidden` 规则 |
| **半硬约束** | 程序化拦截 + 人工确认 | ExecPolicy 的 `prompt` 规则 |
| **软约束** | 依赖模型遵从 `AGENTS.md` 指令，可能被忽略 | "Always run tests before committing" |

---

## 2. 自动化能力全景

```
┌─────────────────────────────────────────────────────┐
│                    Codex CLI 自动化                    │
├──────────────┬──────────────┬───────────────────────┤
│   硬约束      │   软约束      │   外部自动化           │
├──────────────┼──────────────┼───────────────────────┤
│ Hooks        │ AGENTS.md    │ codex exec (CI/CD)    │
│ Rules        │ ├ 工作流指令  │ codex-action (GitHub) │
│ ExecPolicy   │ ├ 完成标准    │ codex mcp-server      │
│ ├ prefix_rule│ └ 反模式约束  │ 远程环境 / Web 任务     │
│ ├ network_rule│              │                       │
│ └ host_exec  │              │                       │
│              │              │                       │
│ notify       │ Skills       │                       │
│ └ 完成通知    │ └ 自动检测   │                       │
│              │              │                       │
│ OS 沙盒      │              │                       │
│ ├ Seatbelt   │              │                       │
│ ├ Landlock   │              │                       │
│ └ seccomp    │              │                       │
│              │              │                       │
│ auto-compact │              │                       │
│ └ 自定义提示  │              │                       │
└──────────────┴──────────────┴───────────────────────┘
```

### 2.1 Starter 默认策略

对这套 guides 而言，**Hooks 应进入参考文档，但不应进入 starter 默认骨架**。

原因很简单：

- 官方 Hooks 文档已公开，说明它是可用能力
- 但官方同时把 Hooks 标成 `Experimental`，并明确说明它仍在 active development
- starter 的职责是提供跨项目都相对稳定的最低配底座，而不是把每个项目都拉进实验能力维护

因此更稳的默认层次是：

1. 先落 `AGENTS.md`
2. 再落 Rules / ExecPolicy
3. 再落 Skills 与 roadmap/spec
4. Hooks 只在项目存在明确、确定性的前后置自动化需求时再单独启用

这不是否定 Hooks，而是把它从“默认必带”降到“项目级增强项”。

---

## 3. ExecPolicy 命令策略（硬约束）

> Codex 最强的自动化机制 — 程序化控制命令执行权限。

### 3.1 概述

ExecPolicy 使用 **Starlark**（类 Python 的安全脚本语言）定义规则，决定 Codex 执行的每个 shell 命令是 `allow`（直接执行）、`prompt`（询问用户）还是 `forbidden`（禁止执行）。

### 3.2 规则文件位置

```
/etc/codex/rules/*.rules        ← 系统级（管理员）
~/.codex/rules/*.rules          ← 用户级（个人）
.codex/rules/*.rules            ← 项目级（团队共享）
```

优先级：项目 > 用户 > 系统。多条规则匹配时，**最严格的决策胜出**（`forbidden` > `prompt` > `allow`）。

用户在 TUI 中手动批准的命令会自动写入 `~/.codex/rules/default.rules`。

### 3.3 三种规则类型

#### prefix_rule — 命令前缀匹配

最常用的规则类型，按命令 token 顺序匹配：

```python
# .codex/rules/default.rules

# === 允许的安全操作 ===

prefix_rule(
    pattern = ["git", "status"],
    decision = "allow",
    justification = "Git status is read-only",
)

prefix_rule(
    pattern = ["git", "log"],
    decision = "allow",
    justification = "Git log is read-only",
)

prefix_rule(
    pattern = ["git", "diff"],
    decision = "allow",
    justification = "Git diff is read-only",
)

prefix_rule(
    pattern = ["git", "add"],
    decision = "allow",
    justification = "Staging files is safe",
)

prefix_rule(
    pattern = ["git", "commit"],
    decision = "allow",
    justification = "Commits are local and reversible",
)

prefix_rule(
    pattern = ["pnpm"],
    decision = "allow",
    justification = "Package manager commands are safe in sandbox",
)

prefix_rule(
    pattern = ["npm", "run"],
    decision = "allow",
    justification = "npm scripts are project-defined",
)

# === 需要确认的操作 ===

prefix_rule(
    pattern = ["git", "push"],
    decision = "prompt",
    justification = "Push affects remote, confirm first",
)

prefix_rule(
    pattern = ["git", "reset"],
    decision = "prompt",
    justification = "Reset can lose work, confirm first",
)

# === 禁止的危险操作 ===

prefix_rule(
    pattern = ["git", "push", ["--force", "-f"]],
    decision = "forbidden",
    justification = "Force push is destructive. Use --force-with-lease if needed.",
)

prefix_rule(
    pattern = ["rm", "-rf"],
    decision = "forbidden",
    justification = "Recursive forced deletion is too dangerous",
)

prefix_rule(
    pattern = [["curl", "wget"], ["|"], ["bash", "sh"]],
    decision = "forbidden",
    justification = "Piping remote scripts to shell is a security risk",
)
```

**pattern 语法**：
- 字符串 `"git"` — 精确匹配一个 token
- 列表 `["--force", "-f"]` — 匹配其中任一（OR 关系）
- 多个元素按顺序匹配命令前缀

#### network_rule — 网络访问控制

```python
# 允许 API 访问
network_rule(
    host = "api.openai.com",
    protocol = "https",
    decision = "allow",
    justification = "OpenAI API access",
)

# 禁止访问内部服务
network_rule(
    host = "internal.corp.com",
    protocol = "https",
    decision = "forbidden",
    justification = "Internal services should not be accessed by Codex",
)
```

#### host_executable — 可执行文件路径绑定

限制某个命令只能从特定路径执行：

```python
host_executable(
    name = "git",
    paths = [
        "/opt/homebrew/bin/git",
        "/usr/bin/git",
    ],
)
```

### 3.4 规则自测

```bash
# 测试单条规则文件
codex execpolicy check --rules .codex/rules/default.rules -- git push --force

# 格式化输出
codex execpolicy check --rules .codex/rules/default.rules --pretty -- rm -rf /

# 合并多个规则文件测试
codex execpolicy check --rules system.rules --rules project.rules -- git reset --hard
```

输出 JSON 示例：
```json
{
  "matchedRules": [
    {
      "prefixRuleMatch": {
        "matchedPrefix": ["git", "push", "--force"],
        "decision": "forbidden",
        "justification": "Force push is destructive."
      }
    }
  ],
  "decision": "forbidden"
}
```

### 3.5 智能脚本拆分

Codex 会智能解析复合命令。例如 `bash -c "git add . && rm -rf /"` 会被拆分为 `git add .` 和 `rm -rf /` 分别评估。如果脚本包含重定向、变量替换、通配符等复杂语法，则作为整体评估。

### 3.6 组织级强制规则

管理员可通过 `requirements.toml` 强制执行规则（注意：只允许 `prompt` 和 `forbidden`，不允许 `allow`）：

```toml
# /etc/codex/requirements.toml
[[rules.prefix_rules]]
pattern = [{ token = "git" }, { token = "push" }, { any_of = ["--force", "-f"] }]
decision = "forbidden"
justification = "Force pushing is prohibited by organization policy"
```

### 3.7 ExecPolicy 的局限

- **只能判断，不能修改** — 无法像 Claude Code 的 `updatedInput` 那样透明修改命令
- **只管命令执行** — 无法在文件写入、prompt 提交等其他事件上触发
- **无法注入自定义逻辑** — 无法像 Claude Code 的 Hook 那样在命令前执行测试脚本

---

## 4. 通知系统（硬约束）

### 4.1 配置方式

```toml
# ~/.codex/config.toml 或 .codex/config.toml
notify = ["python3", "/path/to/notify.py"]
```

通知脚本接收 JSON 作为最后一个 argv 参数。

### 4.2 支持的事件

目前仅支持 **`agent-turn-complete`**（代理回合完成）。

### 4.3 JSON 载荷格式

```json
{
  "type": "agent-turn-complete",
  "thread-id": "uuid",
  "turn-id": "12345",
  "cwd": "/Users/example/project",
  "client": "codex-tui",
  "input-messages": ["帮我实现用户登录功能"],
  "last-assistant-message": "登录功能已实现，所有测试通过。"
}
```

### 4.4 通知脚本示例

**macOS 桌面通知**：

```python
#!/usr/bin/env python3
# ~/.codex/scripts/notify.py
import json, subprocess, sys

data = json.loads(sys.argv[-1])
if data.get("type") == "agent-turn-complete":
    msg = data.get("last-assistant-message", "")[:100]
    subprocess.run([
        "osascript", "-e",
        f'display notification "{msg}" with title "Codex 完成" sound name "Glass"'
    ])
```

**Linux 通知**：

```python
#!/usr/bin/env python3
import json, subprocess, sys

data = json.loads(sys.argv[-1])
if data.get("type") == "agent-turn-complete":
    msg = data.get("last-assistant-message", "")[:100]
    subprocess.run(["notify-send", "Codex 完成", msg])
```

### 4.5 与 Claude Code 的差异

| 维度 | Claude Code | Codex |
|------|------------|-------|
| 事件数 | Stop / Notification / 任意 Hook | 仅 `agent-turn-complete` |
| 触发时机 | 完成时 + 等待输入时 + 自定义 | 仅完成时 |
| 配置粒度 | 按事件类型配置不同 Hook | 只有一个全局 `notify` |
| 异步支持 | `"async": true` | 通知脚本天然异步（不阻塞 Codex） |

---

## 5. AGENTS.md 工作流约束（软约束）

> 没有 Hooks 的情况下，AGENTS.md 是控制 Codex 行为的主要手段。

### 5.1 原理

AGENTS.md 的内容作为系统提示注入到每次对话中。Codex **通常会遵守**这些指令，但与 Hook 不同，模型可能在上下文压力下跳过某些步骤。

### 5.2 工作流指令模板

在项目根 `AGENTS.md` 中添加以下章节：

```markdown
## 工作流规则

### 编码前
- Always explore relevant existing code before writing new code
- Always propose 2-3 implementation approaches and wait for confirmation

### 编码后
- Always run `pnpm test` after modifying source files
- Always run `pnpm lint` after modifying TypeScript/JavaScript files
- Run formatting with `pnpm format` automatically after file changes

### 提交前
- Always run `pnpm test` before any `git commit`; do not commit if tests fail
- Always use specific file paths with `git add`; never use `git add .`
- Always use Conventional Commits format for commit messages

### 提交后
- Do not auto-push; wait for explicit confirmation before `git push`
```

### 5.3 完成标准模板

```markdown
## 完成标准

Before reporting any task as "complete", verify ALL of the following:

1. Run `pnpm test` — all tests pass
2. Run `pnpm lint` — no errors
3. Check edge cases: null values, invalid input, insufficient permissions
4. Confirm changes do not break existing functionality (regression check)
5. Commit message follows Conventional Commits format
```

### 5.4 可靠性对比

| 维度 | AGENTS.md 软约束 (Codex) | Hook 硬约束 (Claude Code) |
|------|------------------------|-------------------------|
| 执行方式 | 模型自觉遵守 | 程序化强制执行 |
| 绕过风险 | 上下文满时可能跳过 | 无法绕过 |
| 验证方式 | 信任模型自报 | Hook 独立验证 |
| 失败模式 | 静默跳过（你可能不知道） | 显式阻断/报错 |

### 5.5 提升软约束可靠性的技巧

1. **用强语气词**：Always / Never / Do not（比 Should / Prefer 更有效）
2. **放在文件前部**：AGENTS.md 越前面的内容遵从度越高
3. **配合 ExecPolicy**：将 `git commit` 设为 `prompt`，在人工确认时检查测试是否通过
4. **保持简洁**：指令太多会稀释效果，只写真正重要的约束
5. **结合 CI/CD**：用 `codex exec` 在 CI 中强制验证，弥补软约束的不足

---

## 6. 上下文自动管理

### 6.1 自动压缩

```toml
model_auto_compact_token_limit = 100000  # 接近此阈值时自动压缩
```

Codex 在对话接近此限制时自动触发压缩：
1. 将完整对话历史交给模型生成摘要
2. 用摘要替换原始历史，释放上下文空间
3. 可在回合前、回合中（上下文溢出时）或用户手动触发

### 6.2 自定义压缩提示

控制压缩时保留什么信息：

```toml
# 方式 1：内联提示
compact_prompt = "Summarize focusing on: code changes made, architectural decisions, outstanding tasks, and test results."

# 方式 2：文件引用（适合复杂提示）
experimental_compact_prompt_file = "/Users/me/.codex/compact-prompt.md"
```

**默认压缩提示**会生成交接摘要，包含：当前进度、关键决策、重要约束、剩余任务。

> **这是 Claude Code PreCompact Hook 的部分替代**。Claude Code 用 Hook 在压缩前执行自定义操作（如保存进度到文件）；Codex 用自定义提示控制压缩内容。前者更灵活（可以写文件），后者更简单（只控制摘要内容）。

### 6.3 手动压缩

```bash
/compact                    # 立即压缩
/compact 请保留最近的代码变更和待办任务    # 带指示压缩
```

---

## 7. CI/CD 自动化（硬约束）

> 无法用 Hooks 实现的自动化，可以在 CI/CD 层面用 `codex exec` 实现。

### 7.1 非交互式执行

```bash
# 基础执行
codex exec "检查代码中的安全漏洞"

# 全自动（可写文件 + 自动审批）
codex exec --full-auto "修复失败的测试"

# JSON 输出（CI 解析）
codex exec --json "分析代码质量" | jq '.final_message'

# 结构化输出
codex exec --output-schema ./schema.json -o ./results.json "提取代码指标"
```

### 7.2 GitHub Action

```yaml
# .github/workflows/codex-review.yml
name: Codex Code Review
on:
  pull_request:
    types: [opened, synchronize]
  issue_comment:
    types: [created]

jobs:
  review:
    if: |
      github.event_name == 'pull_request' ||
      (github.event_name == 'issue_comment' && contains(github.event.comment.body, '@codex'))
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
      issues: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: openai/codex-action@v1
        with:
          prompt: "Review this PR for correctness, security, and test coverage."
          model: "gpt-5.4"
          sandbox: "read-only"
          safety-strategy: "drop-sudo"
```

### 7.3 标签驱动自动化

Codex 官方仓库使用标签触发不同任务：

| 标签 | 触发行为 |
|------|---------|
| `codex-review` | 自动代码审查 |
| `codex-attempt` | 自动修复 Issue 并创建 PR |
| `codex-triage` | 自动分诊 Issue 是否有效 |

Prompt 文件存放在 `.github/codex/labels/` 目录中。

### 7.4 CI 中强制测试门禁

在 CI/CD 中实现 Claude Code「提交前跑测试」的效果：

```yaml
# .github/workflows/codex-test-gate.yml
name: Codex Test Gate
on: pull_request

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: openai/codex-action@v1
        with:
          prompt: |
            Verify this PR:
            1. Run all tests
            2. Run linting
            3. Check for security issues
            Report PASS or FAIL with details.
          sandbox: "workspace-write"
          read-only: false
```

---

## 8. MCP 服务器模式（编程控制）

> 将 Codex 作为 MCP 服务器暴露给其他 AI 代理或自动化管道。

### 8.1 启动

```bash
codex mcp-server                              # 标准 MCP 服务器
npx @modelcontextprotocol/inspector codex mcp-server  # 带调试界面
```

### 8.2 暴露的工具

- `codex()` — 启动新对话
- `codex-reply()` — 继续已有对话

参数支持 `prompt`、`model`、`profile`、`cwd`、`approval-policy`、`sandbox` 等。

### 8.3 自动化场景

- **AI 编排系统**可以通过 MCP 控制多个 Codex 实例并行工作
- **自建自动化管道**可以编程式地给 Codex 分配任务、读取结果
- **与其他 MCP 客户端集成**（如另一个 AI 代理调用 Codex 完成编码子任务）

---

## 9. 常见场景实现方案

### 场景 1：提交前自动跑测试

| 方案 | 实现 | 可靠性 |
|------|------|--------|
| Claude Code | PreToolUse Hook 拦截 `git commit`，执行测试脚本，失败则阻断 | 硬约束 |
| **Codex 方案 A** | AGENTS.md：`Always run pnpm test before git commit` | 软约束 |
| **Codex 方案 B** | ExecPolicy：`git commit` 设为 `prompt`，人工确认时检查 | 半硬约束 |
| **Codex 方案 C** | CI/CD：GitHub Action 在 PR 中自动验证 | 硬约束（事后） |

**推荐组合**：方案 A + B + C 三层防护。

ExecPolicy 配置：
```python
# .codex/rules/default.rules
prefix_rule(
    pattern = ["git", "commit"],
    decision = "prompt",
    justification = "Confirm tests passed before committing",
)
```

AGENTS.md 配置：
```markdown
- Always run `pnpm test` before any `git commit`
- If tests fail, fix the failing tests before retrying the commit
- Never skip tests or use --no-verify
```

---

### 场景 2：写文件后自动格式化

| 方案 | 实现 | 可靠性 |
|------|------|--------|
| Claude Code | PostToolUse Hook 匹配 Write/Edit，自动执行 prettier/black | 硬约束 |
| **Codex 方案** | AGENTS.md：`Run pnpm format after modifying source files` | 软约束 |

```markdown
## AGENTS.md
- After modifying any .ts/.tsx/.js/.jsx file, run `npx prettier --write <file>`
- After modifying any .py file, run `black <file>`
```

> 这是 Codex 当前无法硬性保证的场景。模型通常会遵守，但不是 100%。

---

### 场景 3：会话启动加载上下文

| 方案 | 实现 | 可靠性 |
|------|------|--------|
| Claude Code | SessionStart Hook 执行脚本，显示 git 状态 | 硬约束 |
| **Codex 方案** | AGENTS.md + `/init` 生成的内容 | 部分覆盖 |

Codex 没有 SessionStart 等价功能。替代方案：

1. AGENTS.md 会每次会话自动加载（提供项目规范）
2. 使用 `codex resume --last` 恢复上次对话
3. 自建 `$catchup` Skill，手动触发时读取项目状态

---

### 场景 4：完成通知

| 方案 | 实现 | 可靠性 |
|------|------|--------|
| Claude Code | Stop Hook 发送桌面通知 | 硬约束 |
| **Codex 方案** | `notify` 配置 | 硬约束 ✅ |

```toml
# ~/.codex/config.toml
notify = ["python3", "/Users/me/.codex/scripts/notify.py"]
```

这是 Codex 可以完整实现的场景。

---

### 场景 5：危险命令拦截

| 方案 | 实现 | 可靠性 |
|------|------|--------|
| Claude Code | PreToolUse Hook + permissions.deny | 硬约束 |
| **Codex 方案** | ExecPolicy `forbidden` 规则 | 硬约束 ✅ |

```python
# .codex/rules/default.rules
prefix_rule(
    pattern = ["rm", "-rf", "/"],
    decision = "forbidden",
    justification = "Recursive deletion of root is catastrophic",
)

prefix_rule(
    pattern = ["git", "push", ["--force", "-f"]],
    decision = "forbidden",
    justification = "Force push is destructive. Use --force-with-lease.",
)
```

这是 Codex 可以完整实现的场景，且通过 OS 级沙盒提供额外保护。

---

### 场景 6：上下文压缩前保存进度

| 方案 | 实现 | 可靠性 |
|------|------|--------|
| Claude Code | PreCompact Hook 让 Claude 写文件保存进度 | 硬约束 |
| **Codex 方案** | 自定义 `compact_prompt`，控制摘要包含关键信息 | 半硬约束 |

```toml
compact_prompt = """
Create a handoff summary including:
1. Current progress and completed tasks
2. Key technical decisions and their rationale
3. Outstanding tasks and next steps
4. Important code changes with file paths
5. Any blockers or issues discovered
"""
```

---

## 10. 当前局限与未来展望

### 10.1 仍弱于 Claude Code 的场景

| 场景 | 原因 | 可能的未来解决 |
|------|------|--------------|
| 自动学习并持久化偏好 | 无 Auto Memory 等价 | 继续依赖 `AGENTS.md` 与会话文档 |
| 每次 prompt 前注入上下文 | Codex 仍以 `AGENTS.md`、resume、明确文件路径为主 | 暂未见强等价 |
| 提交前自动跑测试并强制阻断 | Rules 只能决定 allow/prompt/forbidden；复杂逻辑仍需 Hooks + CI | 以官方更新为准 |
| Hook 透明修改命令 | ExecPolicy 只能判断，不能修改 | 暂无 |
| Skill 内定义临时 Hook | 当前官方文档未见 frontmatter hooks | 暂无 |

### 10.2 对这套 guides 的真实影响

本目录在初版里把 Codex 写成“几乎没有 Hooks，只能靠 Rules + AGENTS.md”。这个前提已经过时。更合理的设计应该是：

1. 先用 Hooks 处理前后置脚本、通知、简单自动化。
2. 再用 Rules / ExecPolicy 做高置信的命令门禁。
3. 把无法在本地会话中稳定保证的逻辑放到 CI/CD。
4. 把长期规范写进 `AGENTS.md`，把长期状态写进 roadmap/spec/session-notes。

### 10.3 应对策略

推荐的分层防护策略：

```
第一层：AGENTS.md 软约束（规范、完成标准、边界）
   ↓
第二层：Hooks（前后置动作、提醒、局部自动化）
   ↓
第三层：Rules / ExecPolicy（命令级 allow/prompt/forbidden）
   ↓
第四层：CI/CD 自动化（codex exec + GitHub Action，最终兜底）
   ↓
第五层：MCP / 外部系统（需要编程化控制时）
```

---

## 附录：完整 ExecPolicy 配置示例

```python
# .codex/rules/default.rules
# 项目级命令执行策略

# === Git 操作 ===

prefix_rule(
    pattern = ["git", ["status", "log", "diff", "branch", "show"]],
    decision = "allow",
    justification = "Read-only git operations",
)

prefix_rule(
    pattern = ["git", "add"],
    decision = "allow",
    justification = "Staging files is safe",
)

prefix_rule(
    pattern = ["git", "commit"],
    decision = "prompt",
    justification = "Verify tests passed before committing",
)

prefix_rule(
    pattern = ["git", "push"],
    decision = "prompt",
    justification = "Push affects remote repository",
)

prefix_rule(
    pattern = ["git", "push", ["--force", "-f"]],
    decision = "forbidden",
    justification = "Force push is destructive. Use --force-with-lease.",
)

prefix_rule(
    pattern = ["git", "reset", "--hard"],
    decision = "forbidden",
    justification = "Hard reset discards uncommitted work",
)

# === 构建工具 ===

prefix_rule(
    pattern = ["pnpm"],
    decision = "allow",
    justification = "pnpm commands run in sandbox",
)

prefix_rule(
    pattern = ["npm", "run"],
    decision = "allow",
    justification = "npm scripts are project-defined",
)

# === 危险操作 ===

prefix_rule(
    pattern = ["rm", "-rf"],
    decision = "forbidden",
    justification = "Recursive forced deletion is too dangerous",
)

prefix_rule(
    pattern = [["curl", "wget"], ["|"], ["bash", "sh"]],
    decision = "forbidden",
    justification = "Piping remote scripts to shell is a security risk",
)
```

---

**版本**: v1.3（工作流对齐版）
**更新日期**: 2026-03-31
