# 工作流最佳实践

> 2026 年 Codex CLI 高效协作工作流 — 基于官方文档和社区实践总结

**版本**: v1.3（工作流对齐版）
**适用**: Codex CLI v0.115.0 附近（2026-03）
**对标**: Claude Code guides v3.14《04-工作流最佳实践》

---

## 目录

1. [与 Claude Code 工作流的核心差异](#1-与-claude-code-工作流的核心差异)
2. [核心开发循环](#2-核心开发循环)
3. [Plan Mode 使用规范](#3-plan-mode-使用规范)
4. [多代理协作](#4-多代理协作)
5. [并行开发：Git Worktrees](#5-并行开发git-worktrees)
6. [MCP 服务器集成](#6-mcp-服务器集成)
7. [GitHub Actions 与 CI/CD](#7-github-actions-与-cicd)
8. [上下文管理](#8-上下文管理)
9. [会话管理与恢复](#9-会话管理与恢复)
10. [远程环境与 Web 长任务](#10-远程环境与-web-长任务)
11. [Profiles 多场景切换](#11-profiles-多场景切换)
12. [反模式：避免这些坑](#12-反模式避免这些坑)
13. [快速参考卡](#13-快速参考卡)

---

## 1. 与 Claude Code 工作流的核心差异

| 维度 | Claude Code | Codex CLI |
|------|------------|-----------|
| 开发循环 | Explore → Plan → Code → Verify → Simplify → Commit | Explore → Plan → Code → Verify → Review → Sync State → Commit |
| Plan Mode | `Shift+Tab` / `/plan` | `/plan` |
| 代码审查 | `/simplify`（三维并行 agent） | `/review`（审查预设） |
| 批量变更 | `/batch`（worktree agent 并行） | Subagents / 多线程拆分 / Git Worktree |
| 定时重复 | `/loop 5m /audit --quick` | 无等价功能 |
| 完成通知 | Stop Hook | `notify` + Hooks |
| 自动格式化 | PostToolUse Hook | Hooks + AGENTS.md + CI |
| 测试门禁 | PreToolUse Hook | Hooks / Rules / ExecPolicy + CI |
| 上下文恢复 | Auto Memory 自动恢复 | `codex resume --last` + `$catchup` |
| 并行开发 | `claude --worktree <branch>` | 桌面应用内置 Worktree UI |
| 远程任务 | 无 | Codex Web / 远程环境长任务 |
| 配置切换 | 无 | **Profiles**（`--profile`） |
| 多代理 | Agent Teams（实验性） | Subagents / 多代理协作 |

**最大差异**：
1. **Codex 的原语更分散** — 工作流通常要组合 `AGENTS.md`、Hooks、Rules、Skills、Subagents 和 CI/CD，而不是押注单一能力。
2. **无 Auto Memory** — 每次新会话依然更依赖 `resume`、roadmap、spec、session-notes 这些显式持久化载体。
3. **远程长任务是 Codex 的重要方向** — 最新官方资料更强调 Web / remote environments，而不是只把它理解成一个本地 CLI 附加命令。

---

## 2. 核心开发循环

> 职责边界：本文件解释“为什么这样做、什么时候该切换模式”。具体操作协议、提示词和收尾顺序，以 [00-日常使用说明](./00-%E6%97%A5%E5%B8%B8%E4%BD%BF%E7%94%A8%E8%AF%B4%E6%98%8E.md) 为准。

### 前置阶段：需求讨论（按需）

`$spec` 的价值不是记会议纪要，而是把关键决策持久化成“可实施入口”。当需求会跨多个上下文窗口、多个提交，或者你已经确定了值得保留的决策时，就应该先把讨论结果写进 `docs/specs/*.md`，再进入实施。

更实用的判断是：
- 还不能开始写：继续讨论
- 能开始写，但会跨会话或容易忘决策：先 `$spec`
- 能开始写，而且范围很小：直接进入开发循环

这部分的完整切换协议见 [00-日常使用说明](./00-%E6%97%A5%E5%B8%B8%E4%BD%BF%E7%94%A8%E8%AF%B4%E6%98%8E.md) 的“完整流程演练”。

---

### 开发循环

每个功能进入开发后，遵循七步循环：

```
Explore → Plan → Code → Verify → Review → Sync State → Commit
  探索     规划    编码    验证      审查       状态同步      提交
```

根据复杂度选择合适的流程：

| 复杂度 | 判断标准 | 推荐流程 |
|--------|---------|---------|
| 简单 | 能一句话描述改动 | Code → Verify → Review → Sync State → Commit |
| 中等 | 改多个文件，方向明确 | Explore → Plan → Code → Verify → Review → Sync State → Commit |
| 复杂 | 涉及架构设计、方向不确定 | Explore → Plan → **Clear** → Code → Verify → Review → Sync State → Commit |
| Bug 修复 | 修复已知或新发现的 Bug | Explore（复现+定位）→ Plan（根因确认）→ Code → Verify → Review → Sync State → Commit |

> **复杂功能模式**：Explore + Plan 会消耗大量上下文。此时主动 `/clear` 后带 plan 文件开新会话编码，是**主动策略**而非被动应对。

### Step 1: Explore（探索）

让 Codex 先理解相关代码，**不要急着写代码**。Explore 的目标不是给出实现，而是澄清：
- 相关代码在哪
- 现有约束是什么
- 风险点和不确定点在哪
- 这是不是一个需要先写 spec 或先进入 Plan Mode 的任务

### Step 2: Plan（规划）

Plan 的目标是收敛范围，而不是延长讨论。一个合格的 Plan，至少应该回答：
- 第一批准备改哪些文件
- 第一批明确不改什么
- 先验证什么

如果 Explore + Plan 已经消耗了大量上下文，就把方案写入项目文件后主动 `/clear`，再开新会话实施。何时清、怎么清，见本文第 8 节和 [00-日常使用说明](./00-%E6%97%A5%E5%B8%B8%E4%BD%BF%E7%94%A8%E8%AF%B4%E6%98%8E.md)。

### Step 3: Code（编码）

Code 阶段最重要的不是“赶紧写”，而是控制切片大小。推荐按“这一轮能不能完整验证并收尾”来切，而不是按模块名或文件数来切。

经验规则：
- 一个切片只服务一个明确结果
- 改完后能立即验证
- 验证完能直接进入 review
- 中断时容易 handoff

具体的切片协议和提示词见 [00-日常使用说明](./00-%E6%97%A5%E5%B8%B8%E4%BD%BF%E7%94%A8%E8%AF%B4%E6%98%8E.md)。

### Step 4: Verify（验证）

Verify 在 Codex 里尤其重要，因为它经常是 `AGENTS.md + Hooks + Rules + CI` 共同作用的结果，而不是单一产品能力替你兜底。

判断标准只有一个：Verify 必须产出可检查结果，而不是一句“已验证”。至少要说清：
- 跑了什么
- 没跑什么，以及原因
- 查了哪些边界
- 还剩什么风险

如果这 4 条说不清，通常还不应该进入 review。完整的 Verify 输出格式见 [00-日常使用说明](./00-%E6%97%A5%E5%B8%B8%E4%BD%BF%E7%94%A8%E8%AF%B4%E6%98%8E.md)。

### Step 5: Review（审查）

Review 的目标不是“盖章通过”，而是主动暴露剩余风险。更有价值的 review 输出至少要包含：
- 发现了什么问题
- 哪些已经修复
- 哪些仍建议人工确认
- 还有哪些回归或安全风险

Codex 的 `/review` 更像审查预设，不是 Claude Code `/simplify` 的直接替代，所以更需要你在项目里把 review 标准写清楚。具体汇报格式见 [00-日常使用说明](./00-%E6%97%A5%E5%B8%B8%E4%BD%BF%E7%94%A8%E8%AF%B4%E6%98%8E.md)。

### Step 6: Sync State（同步状态）

Sync State 是 Codex 替代 Auto Memory 的关键环节。与其期待模型自己“记住”，不如把当前状态明确写进：
- `docs/roadmap/`
- `docs/specs/`
- `.agents/session-notes.md`

原则很简单：状态同步应该发生在 commit 前，而不是事后补文档。完整顺序和 `$done / $handoff` 分工，见 [00-日常使用说明](./00-%E6%97%A5%E5%B8%B8%E4%BD%BF%E7%94%A8%E8%AF%B4%E6%98%8E.md)。

### Step 7: Commit（提交）

Commit 不是“代码写完了就顺手做”。只有当当前切片已经完成验证、review 和状态同步，commit 才成立。

这里的工程原则是：
- 用明确文件路径 `git add`
- commit 前先看验证和风险是否收束
- push 继续保留人工确认门槛

### Bug 修复变体

| 步骤 | 新功能 | Bug 修复 |
|------|--------|---------|
| **Explore** | 了解架构，找切入点 | **复现 Bug**，阅读日志/报错，定位根因 |
| **Plan** | 选实现方案 | **确认根因**，评估修复影响 |
| **Code** | 先写测试 → 实现功能 | 先写**复现测试** → 修复 → 测试变绿 |
| **Verify** | 功能正确 + 边界覆盖 | Bug 消失 + **回归验证** |
| **Review** | 检查回归与遗漏 | 检查修复是否引入新副作用 |
| **Sync State** | 更新 Roadmap / Spec | 更新 Roadmap / Spec / 交接信息 |
| **Commit** | `feat:` 前缀 | `fix:` 前缀 |

### 三层收尾模型

| 层级 | 触发方式 | 动作 |
|------|---------|------|
| **Commit 级** | ExecPolicy `prompt`（每次 `git commit`） | 人工确认测试已通过 |
| **功能级** | AGENTS.md 完成标准 + 手动 `$done` | 更新 Roadmap + Spec 状态 + 代码审查 |
| **文档级** | 手动 `$docs` | 刷新开发文档、starter 和入口说明 |
| **Phase / 版本级** | 手动 `$release` | 轻量发布整理 + changelog + 状态同步 |

### 默认入口分工

- `$task`：方向明确、当前会话可闭环的小任务
- `$spec`：会跨多个会话或多个决策点的改动
- `$done`：功能或切片完成后的状态收尾
- `$docs`：文档刷新、starter 回写、开发文档更新
- `$release`：阶段或版本里程碑整理
- `$diagnose`：结构、流程和长期维护风险诊断

---

## 3. Plan Mode 使用规范

### 进入 Plan Mode

```bash
/plan                                    # 进入 Plan Mode
/plan Propose a migration plan for X     # 带指示进入
```

> **与 Claude Code 的差异**：Claude Code 支持 `Shift+Tab` 快捷键切换 Plan/Normal 模式。Codex 通过 `/plan` 命令进入。

### Plan Mode 的价值

| 维度 | 效果 |
|------|------|
| Token 节省 | 分析阶段减少工具调用 |
| 错误率降低 | 先规划再实施 |
| 并发安全 | Plan Mode 下不修改代码，可以并发多个会话 |

### 何时使用

**必须使用**：
- 修改多个文件的功能
- 不熟悉的代码区域
- 有架构影响的变更
- 调试复杂 Bug 前的分析

**不需要**：
- 单文件简单修改
- 明确知道改哪里、怎么改

### Plan Mode 实践技巧

```bash
# 1. 让 Codex 充分探索
/plan 列出所有会受影响的文件和模块

# 2. 要求说明假设
"你假设 X 是这样工作的，这个假设正确吗？"

# 3. 确认后退出 Plan Mode，开始实施
"好，按方案 2 实施"

# 4. 在编辑器中编辑提示
Ctrl+G             # 在 $VISUAL/$EDITOR 中打开，适合长提示
```

---

## 4. 多代理协作

> **复核说明**：官方文档已把 Subagents 作为公开能力单独说明。这里不再沿用早期“实验开关 + 隐藏接口”的写法，而是直接按当前可用的项目级 custom agents / subagents 心智来设计。

### 4.1 默认能力与项目级配置

```toml
# .codex/config.toml
[agents]
max_threads = 6                    # 最大并发线程数
max_depth = 1                      # 嵌套深度（子代理不再生成子代理）
```

项目级 custom agents 放在 `.codex/agents/*.toml`。推荐先把“探索 / 审查 / 文档核实”拆开，而不是一开始就堆很多角色。

### 4.2 内置角色

| 角色 | 用途 | 特性 |
|------|------|------|
| `default` | 通用代理 | 标准能力 |
| `worker` | 执行实现任务 | 编码导向 |
| `explorer` | 读取密集型探索 | 只读操作为主 |

### 4.3 自定义角色

```toml
# .codex/agents/reviewer.toml
name = "reviewer"
description = "资深代码审查员，检查逻辑错误、安全漏洞、回归风险和缺失验证"
model = "gpt-5.4"
model_reasoning_effort = "high"
sandbox_mode = "read-only"
developer_instructions = """
像仓库 owner 一样审查本次改动。
优先发现正确性问题、安全问题、用户可见回归和缺失验证。
先给出具体发现，再给结论。
"""
```

### 4.4 推荐分工

```bash
/agent               # 切换/查看代理线程
# 按 'o' 切换到有审批请求的线程
```

推荐这样拆：

- `explorer`：先定位真实代码路径、依赖关系和影响范围
- `worker`：对单一明确切片做实现或修复
- `reviewer`：对已完成改动做只读审查
- `docs-researcher`：核对官方文档、版本敏感项和外部能力边界

没有内置 `/batch` 等价物时，优先用“主线程定范围 + subagents 并行探索/审查 + Git Worktree 并行实现”的组合。

### 4.5 成本与边界

每个子代理都是独立上下文与独立 Token 消耗。并发数越高，成本和协调复杂度越高。经验原则：

- 只把可并行、边界清楚的任务交给子代理
- 立即阻塞主线程的事情不要委派
- 优先让子代理做只读探索、文档核实和审查
- 真正要并行改代码时，先用 Git Worktree 或清晰的文件 ownership 切开

---

## 5. 并行开发：Git Worktrees

### 5.1 桌面应用（推荐）

Codex 桌面应用（macOS）内置完整的 Worktree UI：

```bash
codex app             # 启动桌面应用
```

**创建 Worktree**：
- 新建线程时选择 "Worktree" 选项
- Codex 在 `$CODEX_HOME/worktrees/` 下创建隔离工作区
- 每个 Worktree 有独立的代码副本和上下文

**两种类型**：

| 类型 | 用途 | 生命周期 |
|------|------|---------|
| Codex-managed | 临时任务，一次性 | 自动清理（保留最近 15 个） |
| Permanent | 长期功能开发 | 手动管理 |

**Handoff 功能**：
- 可以安全地将线程在 Worktree 和本地 checkout 之间迁移
- 删除的 Worktree 会保留快照

### 5.2 CLI 替代方案

CLI 没有专用的 Worktree 命令，但可以用标准 Git Worktree：

```bash
# 手动创建 Worktree
git worktree add ../project-auth feature-auth
cd ../project-auth
codex

# 同时另开一个终端
git worktree add ../project-api feature-api
cd ../project-api
codex
```

### 5.3 多目录授权

```bash
codex --add-dir /path/to/other/project    # 授权额外目录访问
```

### 5.4 并行开发场景

**前端+后端并行**：
```bash
# 终端 1：前端
cd ../project-ui && codex
# → 只加载 apps/web/ 相关文件

# 终端 2：后端 API
cd ../project-api && codex
# → 只加载 apps/api/ 相关文件
```

**功能开发+Bug 修复并行**：
```bash
# 终端 1：新功能开发
cd ../project-feat && codex

# 终端 2：紧急 Bug 修复
cd ../project-fix && codex
```

> **与 Claude Code 的差异**：Claude Code 有 `claude --worktree <branch>` 一行命令创建 Worktree 会话。Codex CLI 需要手动 Git Worktree 操作；桌面应用有 UI 支持。

---

## 6. MCP 服务器集成

### 6.1 MCP 管理命令

```bash
codex mcp add context7 -- npx -y @upstash/context7-mcp@latest    # STDIO 方式
codex mcp add github --url https://api.github.com/mcp             # HTTP 方式
codex mcp get context7                                             # 查看配置
codex mcp list                                                     # 列出所有
codex mcp remove context7                                          # 移除
codex mcp login github                                             # OAuth 登录
codex mcp logout github                                            # 登出
```

会话内管理：
```bash
/mcp                  # 查看 MCP 服务器状态
```

### 6.2 TOML 配置

```toml
# .codex/config.toml 或 ~/.codex/config.toml

# STDIO 方式
[mcp_servers.context7]
command = "npx"
args = ["-y", "@upstash/context7-mcp@latest"]
startup_timeout_sec = 10.0         # 启动超时
tool_timeout_sec = 60.0            # 工具执行超时
enabled_tools = ["search", "summarize"]    # 工具白名单
# disabled_tools = ["dangerous_tool"]      # 工具黑名单

# HTTP 方式
[mcp_servers.github]
url = "https://api.github.com/mcp"
bearer_token_env_var = "GITHUB_TOKEN"

# 可选配置
[mcp_servers.example]
command = "node"
args = ["server.js"]
enabled = true                     # 开关
required = true                    # 不可用时报错（而非静默跳过）
env = { API_KEY = "xxx" }         # 环境变量
cwd = "/path/to/server"           # 工作目录
```

### 6.3 工具白名单/黑名单

```toml
[mcp_servers.large-server]
command = "npx"
args = ["large-mcp-server"]
enabled_tools = ["tool_a", "tool_b"]       # 只启用这些
disabled_tools = ["tool_c"]                 # 禁用这些（在 enabled 之后应用）
```

> **Codex 独有功能**：Claude Code 没有 MCP 工具白名单/黑名单，Codex 的 `enabled_tools` / `disabled_tools` 可以精确控制 MCP 暴露的工具。

### 6.4 推荐的 MCP 服务器

**Context7（强烈推荐）**

将实时、版本精确的库文档注入上下文，防止 Codex 使用过时 API：

```bash
codex mcp add context7 -- npx -y @upstash/context7-mcp@latest
```

**GitHub MCP**

直接在 Codex 中操作 GitHub：

```bash
codex mcp add github --url https://api.github.com/mcp
codex mcp login github
```

### 6.5 Codex 作为 MCP 服务器

```bash
codex mcp-server                                               # 启动
npx @modelcontextprotocol/inspector codex mcp-server           # 带调试界面
```

暴露的工具：
- `codex()` — 启动新对话
- `codex-reply()` — 继续已有对话

适合：AI 编排系统控制多个 Codex 实例、自建自动化管道。

### 6.6 不推荐用 MCP 替代的场景

| 需求 | 推荐方式（比 MCP 更高效） |
|------|------------------------|
| 文件操作 | Codex 内置文件工具 |
| Git 操作 | 直接 `!git` 命令 |
| GitHub 简单查询 | `!gh` CLI |
| 数据库查询 | `!psql` / `!mysql` CLI |

---

## 7. GitHub Actions 与 CI/CD

### 7.1 官方 GitHub Action

```yaml
# .github/workflows/codex-review.yml
name: Codex Code Review
on:
  pull_request:
    types: [opened, synchronize]
  issue_comment:
    types: [created]

jobs:
  review:
    if: |
      github.event_name == 'pull_request' ||
      (github.event_name == 'issue_comment' && contains(github.event.comment.body, '@codex'))
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
      issues: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: openai/codex-action@v1
        with:
          prompt: "Review this PR for correctness, security, and test coverage."
          model: "gpt-5.4"
          sandbox: "read-only"
          safety-strategy: "drop-sudo"
```

### 7.2 使用方式

| 触发 | 效果 |
|------|------|
| 提交 PR | Codex 自动审查，留评论 |
| PR 评论 `@codex` | Codex 响应具体问题 |
| Issue 评论 `@codex <请求>` | Codex 自动实现并创建 PR |

### 7.3 标签驱动自动化

| 标签 | 触发行为 |
|------|---------|
| `codex-review` | 自动代码审查 |
| `codex-attempt` | 自动修复 Issue 并创建 PR |
| `codex-triage` | 自动分诊 Issue |

Prompt 文件存放在 `.github/codex/labels/` 目录中。

### 7.4 非交互式执行（CI/CD 集成）

```bash
# 基础执行
codex exec "检查代码中的安全漏洞"

# 全自动（可写文件 + 自动审批）
codex exec --full-auto "修复失败的测试"

# JSON 输出（CI 解析）
codex exec --json "分析代码质量" | jq '.final_message'

# 结构化输出
codex exec --output-schema ./schema.json -o ./results.json "提取代码指标"

# 临时执行（不保存会话）
codex exec --ephemeral "快速检查"

# 恢复会话（非交互式）
codex exec resume --last
```

---

## 8. 上下文管理

### 8.1 监控原则：70% 规则

```
上下文使用率 < 70%：正常工作
上下文使用率 70-85%：计划会话边界
上下文使用率 > 85%：质量会明显下降，主动处理
```

```bash
/status              # Token 使用统计
/statusline          # 自定义状态栏（含 context-remaining）
```

### 8.2 Document-and-Clear 模式（推荐）

推荐顺序仍然是：

```bash
$handoff
/clear
$catchup
```

也可以用 `codex resume --last` 或手动读取 `.agents/session-notes.md` 恢复，但对于长期项目，显式 handoff/catchup 更稳。具体操作协议见 [00-日常使用说明](./00-%E6%97%A5%E5%B8%B8%E4%BD%BF%E7%94%A8%E8%AF%B4%E6%98%8E.md)。

### 8.3 /compact 的使用

```bash
/compact                                    # 立即压缩
/compact 请保留最近的代码变更和待办任务        # 带指示压缩
```

> **与 Claude Code 的差异**：Claude Code 社区普遍不推荐 `/compact`（有损压缩丢失关键信息）。Codex 的 `/compact` 效果取决于模型压缩质量。**推荐优先使用 Document-and-Clear 模式**，`/compact` 作为备选。

### 8.4 上下文管理命令对照

| 命令 | 用途 | 上下文影响 |
|------|------|-----------|
| `/clear` | 清空对话，重新开始 | 完全清空 |
| `/new` | 新对话，保留同一会话 | 新建对话 |
| `/compact` | 有损压缩，释放空间 | 部分保留 |
| `/fork` | 克隆当前对话到新线程 | 复制保留 |

### 8.5 多会话并行策略

对于大型任务，用多个独立会话比单个大会话更高效：

```bash
# 前端会话（独立上下文）
cd ../project-ui && codex

# 后端会话（独立上下文）
cd ../project-api && codex

# 总效果：各自专注，避免上下文拥挤
```

---

## 9. 会话管理与恢复

### 9.1 会话恢复

```bash
codex resume --last         # 恢复最近一次会话
codex resume --all          # 列出所有可恢复会话（含其他目录）
codex resume SESSION_ID     # 恢复指定会话
codex fork --last           # Fork 上次会话到新线程
codex fork SESSION_ID       # Fork 指定会话
```

### 9.2 会话历史配置

```toml
# ~/.codex/config.toml
[history]
persistence = "save-all"    # save-all | none
max_bytes = 5242880         # 最大 5 MiB
```

### 9.3 推荐的会话管理策略

| 场景 | 推荐操作 |
|------|---------|
| 临时中断，稍后继续 | 直接退出，下次 `codex resume --last` |
| 长时间中断 | `$handoff` 保存进度，下次 `$catchup` 恢复 |
| 上下文快满 | `$handoff` → `/clear` → `$catchup` |
| 想尝试不同方案 | `/fork` 分支对话 |
| 简单新任务 | `/new`（同会话新对话）或直接启动新 Codex |

> **与 Claude Code 的关键差异**：Claude Code 的 Auto Memory 会自动学习你的偏好和纠正，跨会话持续生效。Codex 没有这个能力 — **每次纠正 Codex 的行为后，建议立即在 AGENTS.md 中添加对应约束**，否则下次会话同样的问题会重复出现。

具体什么时候该用 `resume`，什么时候该用 `$catchup`，见 [00-日常使用说明](./00-%E6%97%A5%E5%B8%B8%E4%BD%BF%E7%94%A8%E8%AF%B4%E6%98%8E.md) 的“新开会话 vs 继续任务”。

---

## 10. 远程环境与 Web 长任务

> Codex 独有方向之一。最新官方信息更强调 Web / Environments / Internet Access，而不是本文初版里的 `codex cloud` 命令草稿。

### 10.1 应该怎么理解

如果把 Claude Code 的优势理解为“本地会话里快速闭环”，那 Codex 的一个额外优势就是可以把部分任务放到远程环境里异步跑，适合：

- 长时间执行的修复、迁移、审查或探索任务
- 需要隔离环境、单独上下文或更长运行时间的任务
- 不希望本地终端一直挂着等待的任务

### 10.2 工作流建议

推荐把远程环境视为本地工作流的延伸，而不是替代：

1. 本地先做 Explore / Plan，明确目标与约束。
2. 把边界清晰、可独立推进的任务发给远程环境或 Web。
3. 本地继续做 review、集成、验证与最终提交。

### 10.3 文档使用注意

本节初版里关于 `codex cloud` 的命令示例不再作为主依据。实际操作应以最新 Codex Web、Environments、Internet Access 和 changelog 文档为准。

---

## 11. Profiles 多场景切换

> Codex 独有功能 — Claude Code 没有等价特性。

### 11.1 定义 Profiles

```toml
# ~/.codex/config.toml

profile = "standard"          # 默认 Profile

[profiles.standard]
model = "gpt-5.4"
model_reasoning_effort = "medium"
approval_policy = "on-request"

[profiles.deep-review]
model = "gpt-5.4"
model_reasoning_effort = "high"
approval_policy = "on-request"

[profiles.lightweight]
model = "gpt-5.4-mini"
model_reasoning_effort = "low"
approval_policy = "on-request"

[profiles.full-auto]
model = "gpt-5.4"
approval_policy = "never"
sandbox_mode = "workspace-write"
```

### 11.2 使用场景

```bash
# 日常开发
codex

# 复杂任务用高配
codex --profile deep-review

# 简单任务省 Token
codex --profile lightweight

# 全自动模式（可信环境）
codex --profile full-auto
```

### 11.3 Profile 工作流建议

| 场景 | 推荐 Profile |
|------|-------------|
| 日常编码 | `standard`（默认） |
| 架构设计、复杂调试 | `deep-review`（高推理） |
| 简单格式修改、typo | `lightweight`（省 Token） |
| CI/CD 自动化 | `full-auto`（无审批） |

---

## 12. 反模式：避免这些坑

### 12.1 上下文相关

**不要让上下文超过 85% 才处理**

症状：Codex 开始重复已改的错误，代码质量下降
解决：在 70% 时主动执行 `$handoff` → `/clear` → `$catchup`

**不要依赖 Codex 自动记住你的纠正**

症状：每次新会话同样的错误重复出现
解决：每次纠正后立即在 AGENTS.md 中添加 Always/Never 约束

**不要让 AGENTS.md 超过 32 KiB**

症状：超出部分被截断，某些规则被忽略
解决：拆分到子目录 AGENTS.md；精简内容

---

### 12.2 执行相关

**不要跳过 Explore 直接让 Codex 实现**

症状：实现的代码与现有代码冲突，需要大量返工
解决：始终先 Explore，再 Plan，最后 Code

**不要单次任务范围过大**

症状：Codex 声称"已完成"但实际有大量功能缺失
解决：将任务拆分到 30 分钟人工等效工作量以内

**不要让 Codex 独立控制 git push**

症状：误推送到远程
解决：通过 ExecPolicy 将 `git push` 设为 `prompt`；AGENTS.md 明确"Do not auto-push"

**不要让 Codex 修改测试文件来让测试通过**

症状：测试通过但实现是错的
解决：在 AGENTS.md 加入 `Never modify test files to fit a broken implementation — fix the implementation first`

---

### 12.3 配置相关

**不要启用过多 MCP 服务器**

症状：上下文被工具定义占满
解决：只启用真正需要的 MCP；用 `enabled_tools` 限制工具数量

**不要忽略 ExecPolicy 配置**

症状：Codex 执行了危险命令（`rm -rf`、`git push --force`）
解决：配置 `.codex/rules/default.rules`（参见 02 文档）

**不要在 AGENTS.md 中写太多软约束**

症状：指令太多，Codex 遵从度下降
解决：只写真正重要的 Always/Never 约束，控制在 8 KiB 以内

---

### 12.4 期望相关

**不要期望 Codex 第一次就完全正确**

实际：有经验的开发者估计成功率约 50%，需要迭代
正确期望：像和初级开发者协作——给方向、验结果、给反馈

**不要跳过 Code Review 直接合并**

实际："基本正确"的代码可能有微妙的 Bug
解决：AI 写的代码必须经过人工 Review 或 `/review`

---

## 13. 快速参考卡

### 策略速查

| 场景 | 优先动作 |
|------|---------|
| 需求边界还不清楚 | 继续 Explore / Plan，不急着写 |
| 决策需要跨会话保留 | `$spec` |
| 当前任务可在一轮内验证闭环 | 直接切一个小切片实施 |
| 上下文到 70% 左右 | `$handoff` → `/clear` → `$catchup` |
| 任务只是短暂停顿 | `codex resume --last` |
| 一个 Phase 收尾 | `$deep-audit` |
| 纠正了 Codex 的行为 | 立刻更新 `AGENTS.md` |

### 操作入口

具体命令清单、日常顺序和提示词模板，统一看 [00-日常使用说明](./00-%E6%97%A5%E5%B8%B8%E4%BD%BF%E7%94%A8%E8%AF%B4%E6%98%8E.md)。

### 命令速查

#### 系统命令
| 命令 | 用途 |
|------|------|
| `/plan` | 进入 Plan Mode |
| `/review` | 代码审查 |
| `/status` | Token 使用统计 |
| `/model` | 切换模型 |
| `/permissions` | 运行时切换审批模式 |
| `/personality` | 切换沟通风格 |
| `/skills` | 浏览可用 Skills |
| `/mcp` | 管理 MCP 连接 |
| `/diff` | 查看代码变更 |
| `/fork` | 分支对话 |
| `/new` | 新对话（不清屏） |
| `/clear` | 清空上下文 |
| `/compact` | 有损压缩 |
| `/agent` | 切换代理线程 |
| `/mention` | 附加文件 |
| `/statusline` | 自定义状态栏 |
| `/debug-config` | 配置诊断 |
| `/resume` | 恢复保存的对话 |

#### 自定义 Skills
| 命令 | 用途 |
|------|------|
| `$catchup` | 清空上下文后恢复状态 |
| `$handoff` | 提交变更 + 生成交接文档 |
| `$spec` | 讨论成果整理为设计文档 |
| `$task` | 小任务直接闭环 |
| `$done` | 功能完成收尾检查 |
| `$docs` | 文档刷新和契约回写 |
| `$release` | 阶段或版本整理 |
| `$diagnose` | 工作流和结构性健康诊断 |
| `$audit` | 项目健康检查 |
| `$deep-audit` | 全面深度审计 |

### 快捷键

| 快捷键 | 用途 |
|--------|------|
| `Ctrl+G` | 在 `$VISUAL`/`$EDITOR` 中编辑提示 |
| `Ctrl+L` | 清屏 |
| `Ctrl+C` | 退出/中断 |
| `!command` | 执行 shell 命令 |
| `@filename` | 模糊搜索附加文件 |

### 模型选择建议

| 场景 | 推荐配置 |
|------|---------|
| 日常编码 | 默认模型 + `medium` effort |
| 复杂架构设计 | `--profile deep-review`（高配模型 + `high` effort） |
| 简单格式修改 | `--profile lightweight`（轻量模型 + `low` effort） |
| CI/CD 自动化 | `codex exec --full-auto` |

### 附图输入

```bash
codex -i screenshot.png "这个 UI 有什么问题？"
codex --image img1.png,img2.jpg "对比这两个设计"
```

---

**版本**: v1.3（工作流对齐版）
**更新日期**: 2026-03-31
