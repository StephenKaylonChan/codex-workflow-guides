# v1.3 发布记录

日期：2026-03-31
版本：v1.3
级别：minor

## 高价值变化

- 补齐非 Hooks 默认开发流程，对齐 `guides` 中高频维护入口
- 新增 `task`、`docs`、`release`、`diagnose` 四个默认 Skills，并同步 starter
- 新增 `.codex/agents/` 下的 reviewer / docs-researcher 预设
- 新增 `docs/development/` 骨架，并把它纳入初始化与维护路径
- README、`00-04`、starter README、初始化 prompt 已同步到新默认契约

## 影响范围

- 影响 starter：是
- 影响新项目初始化路径：是
- 影响已有项目升级路径：是，已有项目如要对齐 `v1.3`，应补齐新 Skills、custom agents 和 `docs/development/`

## 相关检查

- 已运行结构与一致性检查，见本次任务汇报
- Hooks 默认策略本次未改变，仍保持非默认层
